{
  "name": "serverless-staking-app",
  "version": "1.0.0",
  "description": "Serverless staking application with Cloudflare Workers, Upstash Redis, and CockroachDB",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev"
  },
  "keywords": [
    "cloudflare-workers",
    "upstash-redis",
    "cockroachdb",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@upstash/redis": "^1.34.9",
    "jsonwebtoken": "^9.0.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3",
    "wrangler": "^4.14.4"
  }
}
